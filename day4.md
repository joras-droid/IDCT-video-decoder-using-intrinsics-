EG: If 'O' is 10, the code for 'K' (110) cannot start with 10.This ensures the decoder reads the bitstream without needing separators, enabling the fastest possible data reconstruction.Bitstream -> Symbols, 1 bit at a time

